---
sidebar_position: 1
---

# Variables

In Crabby, a variable is declared with the `let` and `var` keyword.

```js
let a = "hello"
var b = "\nworld"

print(a + b)
```

Output:

```bash
hello world
```
