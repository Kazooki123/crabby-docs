---
sidebar_position: 1
---

# Variables

In Crabby, a variable is declared with the `let` and `var` keyword.

```js
let a = "Hello, "
var b = " World!"

print(a + b)
```

Output:

```bash
Hello, World!
```
